global-variables:
  - &clone
    name: clone
    image: alpine/git
    commands:
    - git clone -b "$DRONE_BRANCH" "$DRONE_REMOTE_URL" .

kind: pipeline
type: kubernetes
name: release

clone:
  depth: 1

trigger:
  branch:
  - release/*
  event:
  - push
